/**
 * The one sample-prompt set, shared by the dashboard empty state and the
 * playground chips so a user never sees two different "try one of these"
 * lists. Short enough for chips, concrete enough to produce a good rewrite.
 */
export const SAMPLE_PROMPTS = [
  'Write a cover letter for a marketing job',
  'Help me refactor this auth code',
  'Summarize this article for a non-technical reader',
]
